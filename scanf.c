#include <stdio.h>


int main(void)
{
    char s[4];
    printf("s: ");
    scanf("%s", s);
    printf("s: %s", s);

    // int n;
    // printf("n: ");
    // // scanf if receiving value input from user, and stored to n variable
    // // to be able to change the value at the variable n, we must know the address of n
    // // so n must included ampersand &;
    // scanf("%i", &n);
    // printf("n: %i", n);
}